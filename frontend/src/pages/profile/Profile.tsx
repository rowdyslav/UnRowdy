import ProfileCard from "@/widgets/profile/ProfileCard.tsx";

const Profile = () => {

  return (
    <section className='container'>
      <ProfileCard/>
    </section>
  );
};

export default Profile;